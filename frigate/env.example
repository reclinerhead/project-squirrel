# Copy to frigate/.env on pearl (gitignored by the root .env* rule).
# The Amcrest RTSP password -- the same secret bluejay carries as
# MERLE_RTSP_PASS. Frigate substitutes {FRIGATE_RTSP_PASSWORD} in config.yml.
FRIGATE_RTSP_PASSWORD=
