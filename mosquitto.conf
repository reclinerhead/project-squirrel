# project-squirrel -- mosquitto.conf (RETIRED POINTER)
#
# The Merle event bus no longer runs on this machine. Mosquitto was
# uninstalled from bluejay; the broker lives on pearl (192.168.1.64, the
# always-on Ubuntu box), configured at:
#
#   pearl:/etc/mosquitto/conf.d/squirrel.conf
#
# Same two listeners as before: 1883 plain MQTT (Python processes, paho-mqtt)
# and 9001 WebSockets (the MCC dashboard in the browser). Anonymous auth; the
# bus never leaves the LAN.
#
# Point processes at it with:
#   MERLE_MQTT=192.168.1.64:1883                       (daemon, replay, narrator)
#   NEXT_PUBLIC_MERLE_MQTT_WS=ws://192.168.1.64:9001   (mcc/.env.local)
